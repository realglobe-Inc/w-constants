/**
 * Test for WConstants.
 * Runs with mocha.
 */
'use strict'

const WConstants = require('../lib/WConstants')
const { ok, equal } = require('assert')

describe('w-constants', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(WConstants)
  })
})

/* global describe, before, after, it */
